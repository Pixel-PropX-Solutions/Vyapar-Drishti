import { ScrollView } from "react-native-gesture-handler";
import TextTheme from "../Components/Text/TextTheme";

export default function NotificationScreen(): React.JSX.Element {
    return (
        <ScrollView>
            <TextTheme>Notification screen</TextTheme>
        </ScrollView>
    )
}