import { ScrollView } from "react-native-gesture-handler";
import TextTheme from "../Components/Text/TextTheme";

export default function SettingScreen(): React.JSX.Element {
    return (
        <ScrollView>
            <TextTheme>Setting screen</TextTheme>
        </ScrollView>
    )
}